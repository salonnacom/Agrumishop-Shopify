{% for group in robots.default_groups %}
  {{- group.user_agent -}}

  {% for rule in group.rules %}
    {{- rule -}}
  {% endfor %}

  {{ group.sitemap }}

{% endfor %}

# Custom rules for SEO
User-agent: *
Disallow: /*?pr_
Disallow: /*?*utm_